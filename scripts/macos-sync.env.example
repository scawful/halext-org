# Optional configuration file for scripts/macos-sync.sh
# Copy to scripts/macos-sync.env (gitignored) and update values.

# Force a specific restart mode (auto, dev, launchd)
# HALX_MAC_MODE=launchd

# Remote server settings for --server-sync
# HALX_REMOTE_SERVER="halext@org.halext.org"
# HALX_REMOTE_SERVER_DIR="/srv/halext/halext-org"
# HALX_REMOTE_SERVER_SCRIPT="./scripts/server-sync.sh"
# HALX_REMOTE_SERVER_ENV="HALX_DOMAIN=org.halext.org"
# HALX_REMOTE_SSH_OPTS="-p 22 -o StrictHostKeyChecking=no"
