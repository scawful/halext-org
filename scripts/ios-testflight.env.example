# iOS TestFlight Deployment Configuration
# Copy this file to ios-testflight.env and fill in your values
# IMPORTANT: Never commit the actual .env file (it's in .gitignore)

# ==============================================================================
# App Store Connect API Authentication
# ==============================================================================
# Generate these in App Store Connect:
# Users and Access > Integrations > App Store Connect API > Generate API Key
#
# Download the .p8 key file and store it securely.
# The key file should have restricted permissions (chmod 600)

# API Key ID (from App Store Connect, e.g., "ABC123XYZ")
APP_STORE_CONNECT_KEY_ID=""

# Issuer ID (from App Store Connect API keys page, e.g., "12345678-abcd-1234-efgh-123456789012")
APP_STORE_CONNECT_ISSUER_ID=""

# Path to your .p8 private key file
# Recommended: Store in ~/.appstoreconnect/AuthKey_<KEY_ID>.p8
APP_STORE_CONNECT_KEY_PATH="$HOME/.appstoreconnect/AuthKey_KEYID.p8"

# ==============================================================================
# Alternative: App-Specific Password (Legacy method via altool)
# ==============================================================================
# If you prefer using xcrun altool instead of the App Store Connect API:
# 1. Go to https://appleid.apple.com
# 2. Security > App-Specific Passwords > Generate Password
#
# Note: App Store Connect API is the recommended modern approach

# Your Apple ID email
APPLE_ID=""

# App-specific password (NOT your Apple ID password)
APPLE_APP_SPECIFIC_PASSWORD=""

# ==============================================================================
# Build Configuration
# ==============================================================================

# Team ID for code signing (found in Apple Developer Portal)
DEVELOPMENT_TEAM=""

# Bundle Identifier (should match Xcode project)
BUNDLE_IDENTIFIER="org.halext.Cafe"

# Xcode project and scheme
XCODE_PROJECT="Cafe.xcodeproj"
XCODE_SCHEME="Cafe"

# ==============================================================================
# Notification Settings (Optional)
# ==============================================================================

# Slack webhook URL for deployment notifications
SLACK_WEBHOOK_URL=""

# Discord webhook URL for deployment notifications
DISCORD_WEBHOOK_URL=""

# ==============================================================================
# Advanced Options
# ==============================================================================

# Export method: app-store, ad-hoc, enterprise, development
EXPORT_METHOD="app-store"

# Upload symbols to Apple for crash reporting
UPLOAD_SYMBOLS="true"

# Include bitcode (deprecated as of Xcode 14)
INCLUDE_BITCODE="false"

# Compile bitcode (deprecated as of Xcode 14)
COMPILE_BITCODE="false"

# Strip Swift symbols
STRIP_SWIFT_SYMBOLS="true"

# Timeout for upload (in seconds)
UPLOAD_TIMEOUT=3600
