# Configuration for local frontend deployment
# Copy this file to scripts/frontend-deploy.env and adjust as needed.

# SSH connection string (e.g., user@hostname or ~/.ssh/config alias)
HALX_REMOTE="halext@org.halext.org"

# Remote directory where the site is served (must exist)
HALX_REMOTE_DIR="/var/www/halext"

# Optional: Command to run on the server after syncing files
# e.g., "sudo systemctl reload nginx"
# HALX_POST_DEPLOY=""

