# Network Configuration - DO NOT COMMIT THIS FILE WITH REAL VALUES!
# Copy this to .env.network and fill in your actual values
# Add .env.network to .gitignore

# Your public IP addresses (find with: curl -4 ifconfig.me)
PUBLIC_IPV4=YOUR_PUBLIC_IP
PUBLIC_IPV6=YOUR_PUBLIC_IPV6

# Your local network IPs
MAC_LOCAL_IP=YOUR_MAC_LOCAL_IP
WINDOWS_LOCAL_IP=YOUR_WINDOWS_LOCAL_IP

# DDNS hostname (if using DuckDNS, No-IP, etc.)
DDNS_HOSTNAME=yourname.duckdns.org

# Port mappings (if using different external ports)
MAC_EXTERNAL_PORT=11434
MAC_INTERNAL_PORT=11434
WINDOWS_EXTERNAL_PORT=11435
WINDOWS_INTERNAL_PORT=11434
