# Halext Org Backend - Development Environment Variables

# Development mode (disables access code requirement)
DEV_MODE=true

# Database URL (defaults to SQLite if not set)
# DATABASE_URL=sqlite:///./halext_dev.db

# For production PostgreSQL:
# DATABASE_URL=postgresql://halext_user:password@localhost/halext_org

# Access code for user registration (not required in DEV_MODE)
# ACCESS_CODE=your_access_code_here

# JWT Authentication (REQUIRED for production)
# Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Key Encryption (REQUIRED for production)
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your-encryption-key-change-in-production

# AI Provider (mock, openwebui, or ollama)
AI_PROVIDER=mock
AI_MODEL=llama3.1

# OpenWebUI Configuration (if using)
# OPENWEBUI_URL=http://localhost:3000
# OPENWEBUI_PUBLIC_URL=https://org.halext.org/webui/

# Ollama Configuration (if using)
# OLLAMA_URL=http://localhost:11434

# OpenWebUI User Sync (optional)
# OPENWEBUI_SYNC_ENABLED=true
# OPENWEBUI_ADMIN_EMAIL=admin@halext.org
# OPENWEBUI_ADMIN_PASSWORD=your_admin_password

# JWT Secret for SSO (generate a secure random string)
# JWT_SECRET_KEY=your-secret-key-change-in-production
